package org.redstonechips.event;

import org.bukkit.event.Event;

/**
 * A listener interface for general bukkit events.
 * 
 * @author taleisenberg
 */
public interface EventListener {
    public void onEvent(Event e);
}
